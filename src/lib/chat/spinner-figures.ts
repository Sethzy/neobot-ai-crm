/**
 * Unicode figure constants for the spinner system.
 * Ported from cc-src/constants/figures.ts — only the constants needed by
 * the spinner are included.
 * @module lib/chat/spinner-figures
 */

/** The teardrop asterisk `✻` — used as the animated spinner glyph and logo. */
export const TEARDROP_ASTERISK = '✻'
