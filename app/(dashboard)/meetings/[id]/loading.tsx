/**
 * Route-level loading shell for the meeting detail route.
 * @module app/(dashboard)/meetings/[id]/loading
 */
import { MeetingDetailLoading } from "@/components/meetings/meeting-detail-loading";

export default function Loading() {
  return <MeetingDetailLoading />;
}
