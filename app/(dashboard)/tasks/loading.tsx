/**
 * Route-level loading shell for the Tasks workspace.
 * @module app/(dashboard)/tasks/loading
 */
import { TasksPageLoading } from "@/components/tasks/tasks-page-loading";

export default function Loading() {
  return <TasksPageLoading />;
}
